import { questionAnswerCount } from 'stuck-overflow/helpers/question-answer-count';
import { module, test } from 'qunit';

module('Unit | Helper | question answer count');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionAnswerCount([42]);
  assert.ok(result);
});
